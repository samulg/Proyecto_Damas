#include "Ficha.h"
Ficha::Ficha()
{

}
void Ficha::dibujarFicha() {
	
	/*
	
	//por alguna misteriosa razón, he tenido que quitar el -0.5 para que se coloquen correctamente
	//Supongo que el origen de coordenadas está situado ahora en el centro de la casilla inferior izquierda
	posicion.x = (-c);
	posicion.y = (-f);
	glTranslatef((posicion.x), (posicion.y), 0);
	glutSolidCube(0.5);
	glTranslatef((-posicion.x), (-posicion.y), 0);
	*/

}
Ficha::~Ficha()
{

}